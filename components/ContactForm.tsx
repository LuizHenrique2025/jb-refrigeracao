'use client';
import {useState} from 'react';
import {ArrowUpRight,MessageCircle} from 'lucide-react';
export default function ContactForm(){const [sent,setSent]=useState(false);function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);const text=`Olá, João! Meu nome é ${data.get('name')}. Preciso de ${data.get('service')} para um ambiente ${data.get('type')}. ${data.get('message')}`;window.open(`https://wa.me/554598539211?text=${encodeURIComponent(text)}`,'_blank','noopener,noreferrer');setSent(true);}return <form className="contact-form" onSubmit={submit}><div className="form-heading"><p className="eyebrow">FALE SOBRE O SEU AMBIENTE</p><h3>Como podemos ajudar?</h3><p>Um pouco de contexto ajuda o João a entender o melhor caminho.</p></div><div className="form-row"><label>Seu nome<input required name="name" placeholder="Como você se chama?" /></label><label>Atendimento<input name="type" placeholder="Residencial, comercial..." /></label></div><label>O que você precisa<select required name="service" defaultValue=""><option value="" disabled>Selecione uma opção</option><option>Instalação de ar-condicionado</option><option>Manutenção e limpeza</option><option>Diagnóstico de problema</option><option>Sistema central de resfriamento</option></select></label><label>Conte um pouco sobre o serviço<textarea name="message" placeholder="Pode incluir o ambiente, equipamento e o que está acontecendo." rows={4}/></label><button className="form-submit" type="submit"><MessageCircle size={17}/>{sent?'Mensagem pronta no WhatsApp':'Continuar no WhatsApp'}<ArrowUpRight size={17}/></button><small>Você poderá revisar a mensagem antes de enviar.</small></form>}
